const colors = require('tailwindcss/colors')

module.exports = {
  content: [],
  theme: {
    extend: {
      colors: {
        cyan: colors.cyan,
        indigo: colors.indigo,
        teal: colors.teal,
      },
    },
  },
  plugins: [],
}
